"""python -m pip install -U pip
if (Test-Path .\requirements.txt) {
  python -m pip install -r .\requirements.txt
} else {
  # minimal common stack
  python -m pip install streamlit yfinance pandas plotly
}
# --- edit these if you move the repo ---

$Repo = "C:\Users\ian\Desktop\Program\QuantStack\QuantStack-main"
$App  = Join-Path $Repo "app.py"
$Venv = Join-Path $Repo "venv"

# Use local policy so activation works in this session
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned -Force

# Ensure venv exists
if (-not (Test-Path $Venv)) {
  & python -m venv $Venv
}

# Activate venv
& "$Venv\Scripts\Activate.ps1"

# Upgrade pip and install deps
& python -m pip install -U pip
if (Test-Path (Join-Path $Repo "requirements.txt")) {
  & python -m pip install -r (Join-Path $Repo "requirements.txt")
} else {
  & python -m pip install streamlit yfinance pandas plotly
}

# Run Streamlit (change port if you like)
& python -m streamlit run $App --server.port 8501



C:\Windows\System32\cmd.exe /k "cd /d C:\Users\ian\Desktop\Program\QuantStack\QuantStack-main && call venv\Scripts\activate.bat && python -m streamlit run app.py --server.address 127.0.0.1 --server.port 8501"
"""
